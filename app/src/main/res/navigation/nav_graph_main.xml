<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_main"
    app:startDestination="@id/tableFragment">

    <fragment
        android:id="@+id/scheduleFragment"
        android:name="com.senex.timetable.presentation.groups.GroupsFragment"
        android:label="fragment_groups"
        tools:layout="@layout/fragment_groups" >
        <action
            android:id="@+id/action_groupsFragment_to_tableFragment"
            app:destination="@id/tableFragment" />
    </fragment>
    <fragment
        android:id="@+id/tableFragment"
        android:name="com.senex.timetable.presentation.schedule.ScheduleFragment"
        android:label="fragment_table"
        tools:layout="@layout/fragment_schedule" >
        <action
            android:id="@+id/action_tableFragment_to_groupsFragment"
            app:destination="@id/scheduleFragment" />
    </fragment>
</navigation>