<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application appTheme. -->
    <style name="Theme.ItisTimetable" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/background_light</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>

        <!-- Tab bar and nav drawer backgrounds -->
        <item name="colorSurface">@color/main_background_light</item>

        <item name="android:colorBackground">@color/main_background</item>

        <!-- Emphasized items on default background -->
        <item name="android:colorBackgroundFloating">@color/color_background_floating</item>

        <item name="color">@color/main_background</item>

        <item name="drawerArrowStyle">@style/NavigationArrowStyle</item>
    </style>

    <style name="TextStyle" />
    <style name="TextStyle.Body" parent="TextAppearance.Material3.TitleMedium" />

    <style name="NavigationArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/primary</item>
    </style>
</resources>